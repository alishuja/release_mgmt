@if(Request.IsAuthenticated) {
    @Html.ActionLink("Log Off", "LogOff", "Account", null, new { @class = "buttonlink right" }) 
}
else {
     @Html.ActionLink("Log On", "LogOn", "Account", null, new { @class = "buttonlink right" })
}
